// Swap Caps Lock and Super keys
partial modifier_keys
xkb_symbols "swap_caps_super" {
    key <CAPS> { [ Super_L ] };
    key <LWIN> { [ Caps_Lock ] };
    key <RWIN> { [ Caps_Lock ] };
    modifier_map Mod4 { <CAPS> };
    modifier_map Lock { <LWIN>, <RWIN> };
};

// The ; -> BackSpace remap used to live here as "semicolon_backspace".
// It moved to keyd (keyboards/keyd/default.conf) because putting BackSpace on
// level 1 of <AC10> made Chromium tag every press of that key VKEY_BACK,
// which broke Shift+; -> ':' in contenteditables. Do not reintroduce it here.
